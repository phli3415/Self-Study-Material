import { Button } from 'antd';
function App(){
  return <div>
    this is div
    <Button type='primary'>test</Button>
    {/* type="primary" 会使按钮呈现为主要的、突出的样式。通常，
    它会使用主题的主色（通常是蓝色），使其在页面上更加醒目。 */}
    
  </div>
}

export default App