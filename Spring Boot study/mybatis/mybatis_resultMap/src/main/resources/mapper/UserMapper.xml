<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.atguigu.mybatis.mapper.UserMapper">

    <insert id="insertUser">
        insert into t_user value(null, '123', 111111, 23, 'm', '1234@gmail.com' )
    </insert>


    <update id="updateUser">
        update t_user set username='root', password='123' where id=3
    </update>


    <delete id="deleteUser">
        delete from t_user where id=3
    </delete>


    <select id="getUserById" resultType="com.atguigu.mybatis.pojo.user">
        /*设置了typeAlias后可以用abc代替resultType*/
        select* from t_user where id=2
    </select>


    <select id="getUserList" resultType="com.atguigu.mybatis.pojo.user">
        select* from t_user
    </select>

    <select id="getUserByName" resultType="com.atguigu.mybatis.pojo.user">
        <!--select * from t_user where username= #{name}-->
        <!--sql底层不知道传过来的变量名是什么，#{}你写什么都可以-->
         select * from t_user where username= '${name}'
        <!--$要引号-->


    </select>

    <!--    若mapper接口中的方法参数为多个时

    此时MyBatis会自动将这些参数放在一个map集合中，以arg0,arg1...为键，以参数为值；以

    param1,param2...为键，以参数为值；因此只需要通过${}和#{}访问map集合的键就可以获取相

    对应的值，注意${}需要手动加单引号-->
    <!--    底层参数名: arg0,1....   param1,2....-->
    <!--    底层会把这些数值放在一个map里， arg#, param# 是键-->
    <select id="checkLogin" resultType="com.atguigu.mybatis.pojo.user">
        select * from t_user where username= #{arg0} and password=#{arg1}

    </select>

    <select id="checkLoginByMap" resultType="com.atguigu.mybatis.pojo.user">
        select * from t_user where username= #{username} and password=#{password}
    </select>
<!--    直接放map的key就行-->




    <!--    传object时属性名不一定是你设的那个-->
    <!--    get， set 去掉get/set 首字母转小写-->
    <!--    查数据时看getter， 改数据时看setter-->
    <!--    若mapper接口中的方法参数为实体类对象时

    此时可以使用{}和#{}，通过访问实体类对象中的属性名获取属性值，注意
    {}需要手动加单引号-->

    <insert id="insertUserByClass">
        insert into t_user values(null, #{userName}, #{password}, #{age}, #{gender}, #{email})
    </insert>


<!--可以通过@Param注解标识mapper接口中的方法参数

此时，会将这些参数放在map集合中，以@Param注解的value属性值为键，以参数为值；以

param1,param2...为键，以参数为值；只需要通过${}和#{}访问map集合的键就可以获取相对应

的值，

注意${}需要手动加单引号-->
    <select id="checkLoginByParam" resultType="com.atguigu.mybatis.pojo.user">
        select * from t_user where username= #{username} and password=#{password}

    </select>



</mapper>