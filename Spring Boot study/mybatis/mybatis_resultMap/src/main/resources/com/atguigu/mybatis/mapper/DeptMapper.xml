<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.atguigu.mybatis.mapper.DeptMapper">

<!--    Dept getEmpDeptByStepTwo(@Param("id") Integer id);-->
    <select id="getEmpDeptByStepTwo" resultType="com.atguigu.mybatis.pojo.Dept">
        select * from t_dept where dept_id=#{id}
    </select>


<!--      Dept getDeptAndEmpByDeptId(@Param("id") Integer id);  -->
    <resultMap id="deptEmpIdResultMap" type="com.atguigu.mybatis.pojo.Dept">
        <id property="deptId" column="dept_id"></id>
        <result property="deptName" column="dept_name"></result>
        <collection property="emps" ofType="Emp">
            <id column="emp_id" property="empId"></id>
            <result column="emp_name" property="empName"></result>
            <result column="age" property="age"></result>
            <result column="gender" property="gender"></result>
        </collection>
<!--        ofType：设置list里的数据类型-->

    </resultMap>
<!--    collection：处理一对多的关系-->
    <select id="getDeptAndEmpByDeptId" resultMap="deptEmpIdResultMap">
        select * from t_dept
        left join t_emp on
        t_dept.dept_id = t_emp.dept_id where t_dept.dept_id=#{id};
    </select>



    <resultMap id="deptAndEmpResultMapByStep" type="Dept">
        <id column="dept_id" property="deptId"></id>
        <result column="dept_name" property="deptName"></result>
        <collection property="emps"
                    select="com.atguigu.mybatis.mapper.EmpMapper.getDeptAndEmpByStepTwo"
                    column="dept_id">
        </collection>
    </resultMap>
<!--        Dept getDeptAndEmpByStepOne(@Param("id") Integer id);-->
    <select id="getDeptAndEmpByStepOne" resultMap="">
        select * from t_dept where dept_id = #{id}
    </select>

</mapper>